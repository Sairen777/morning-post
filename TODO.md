1. adress all TODO in project (but not in other .md files)
2. make linter/prettier to not use one line conditions like `if (something) return`, always use curly braces
3. make linter/prettier add newline before if and return statements
4. Make a plan for all fixes
